# Zoom API Credentials (General App - OAuth)
ZOOM_CLIENT_ID=your_client_id
ZOOM_CLIENT_SECRET=your_client_secret

# Optional: Only needed for Server-to-Server OAuth apps (legacy)
# ZOOM_ACCOUNT_ID=your_account_id
